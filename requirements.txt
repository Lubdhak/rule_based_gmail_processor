# Google API client and auth libraries
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Async HTTP client
aiohttp>=3.8.0
aiosignal>=1.2.0          # dependency of aiohttp
async-timeout>=4.0.0      # dependency of aiohttp
attrs>=21.0.0             # dependency of aiohttp
frozenlist>=1.2.0         # dependency of aiohttp
multidict>=5.0.0          # dependency of aiohttp
yarl>=1.7.0               # dependency of aiohttp

# Utilities
python-dateutil>=2.8.0
typing-extensions>=4.0.0

# Standard library modules — no need to install:
# os, json, sqlite3, re, logging, base64, email, datetime, asyncio
